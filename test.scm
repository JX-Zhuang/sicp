(define size 5)
(* size 2)
(define pi 3.14159)
(define radius 10)
(* pi (* radius radius))
(define (square x)
    (* x x))
(define (sum-of-squares x y)
    (+ (square x) (square y)))
(sum-of-squares 3 4)











